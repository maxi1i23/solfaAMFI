import 'package:flutter/material.dart';

class PageAPROPOS extends StatelessWidget {
  const PageAPROPOS({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("page d'A propos"),
      ),
    );
  }
}
